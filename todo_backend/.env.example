# Todo Backend - Environment Variables Example
# Copy this file to .env and adjust values as necessary.

# Persistence backend to use: "memory" (default) or "sqlite"
PERSISTENCE_BACKEND=memory

# Path to SQLite database file (used only when PERSISTENCE_BACKEND=sqlite)
# Relative paths are resolved from the todo_backend working directory.
SQLITE_DB_PATH=./data/todos.db

# CORS allowed origins
# - Use "*" to allow all origins (default)
# - Or provide a comma-separated list, e.g.:
#   CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CORS_ALLOW_ORIGINS=*

# Optional: Enable basic auth for all endpoints (not implemented by default).
# These are placeholders to show available configuration flags if you add auth later.
# Set to "true" to enable, otherwise leave "false" or unset.
ENABLE_BASIC_AUTH=false
BASIC_AUTH_USERNAME=admin
BASIC_AUTH_PASSWORD=change-me
