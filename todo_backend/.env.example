# FastAPI Todo Backend - Example Environment Variables
# Copy this file to .env and adjust values as needed.

# Storage backend: "memory" (default) or "sqlite"
PERSISTENCE_BACKEND=memory

# SQLite database file path (used when PERSISTENCE_BACKEND=sqlite)
SQLITE_DB_PATH=./data/todos.db

# CORS allowed origins:
# - Use "*" to allow all
# - Or provide comma-separated origins, e.g. "http://localhost:5173,http://127.0.0.1:5173"
CORS_ALLOW_ORIGINS=*
